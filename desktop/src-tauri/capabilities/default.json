{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Open local model and report selections.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
