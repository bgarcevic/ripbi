name: Release

on:
  push:
    tags: ["v*"]

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - os: macos-latest
            target: aarch64-apple-darwin
          - os: windows-latest
            target: x86_64-pc-windows-msvc
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2
      - run: cargo build --release --locked -p ripbi --target ${{ matrix.target }}

      - name: Package (tar.gz)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          version="${GITHUB_REF_NAME#v}"
          stage="dist/ripbi-${version}"
          mkdir -p "$stage"
          cp "target/${{ matrix.target }}/release/ripbi" "$stage/"
          cp README.md LICENSE-MIT LICENSE-APACHE "$stage/"
          tar -czf "dist/ripbi-${version}-${{ matrix.target }}.tar.gz" -C dist "ripbi-${version}"

      - name: Package (zip)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $version = $env:GITHUB_REF_NAME.Substring(1)
          $stage = "dist/ripbi-$version"
          New-Item -ItemType Directory -Force -Path $stage | Out-Null
          Copy-Item "target/${{ matrix.target }}/release/ripbi.exe" $stage
          Copy-Item README.md, LICENSE-MIT, LICENSE-APACHE $stage
          Compress-Archive -Path $stage -DestinationPath "dist/ripbi-$version-${{ matrix.target }}.zip"

      - uses: actions/upload-artifact@v4
        with:
          name: dist-${{ matrix.target }}
          path: dist/*
          if-no-files-found: error

  release:
    name: Publish release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          merge-multiple: true
          path: dist

      - name: Checksums
        shell: bash
        run: |
          cd dist
          sha256sum ripbi-* > sha256sums.txt

      - name: Create GitHub release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" dist/* \
            --title "$GITHUB_REF_NAME" \
            --generate-notes \
            --verify-tag

  # Skips until the CARGO_REGISTRY_TOKEN secret exists; once the owner sets it,
  # every tag publish runs. Secrets cannot be tested in a job-level `if`, so the
  # token is mapped into the job env and the steps check it there.
  publish-crates:
    name: Publish to crates.io
    needs: release
    runs-on: ubuntu-latest
    env:
      CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - name: Publish ripbi-core
        if: env.CARGO_REGISTRY_TOKEN != ''
        run: cargo publish --locked -p ripbi-core
      - name: Publish ripbi
        if: env.CARGO_REGISTRY_TOKEN != ''
        shell: bash
        run: |
          # ripbi depends on ripbi-core; the sparse index may not serve the
          # version published seconds ago, so retry a few times before giving up.
          for attempt in 1 2 3 4; do
            if cargo publish --locked -p ripbi; then
              exit 0
            fi
            echo "attempt $attempt failed; ripbi-core may not be visible on the index yet"
            sleep 30
          done
          exit 1
